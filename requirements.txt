# =============================================================================
# APEX — Python dependencies
# pin major.minor; let patch float for security fixes.
# =============================================================================

# --- Core framework ---
pydantic>=2.9,<3
pydantic-settings>=2.6,<3

# --- Async database (TimescaleDB / PostgreSQL) ---
sqlalchemy[asyncio]>=2.0,<3
asyncpg>=0.30,<1
alembic>=1.14,<2

# --- Redis Streams ---
redis[hiredis]>=5.2,<6

# --- HTTP / data ---
httpx>=0.28,<1
aiohttp>=3.11,<4

# --- AI / LLM ---
anthropic>=0.42,<1

# --- Data & analysis ---
pandas>=2.2,<3
numpy>=2.1,<3
pandas-ta>=0.3,<1       # Pure Python TA library (no C deps)

# --- Cross-exchange data ---
ccxt>=4.0,<5            # Binance/Bybit funding rates for arb scanner

# --- Exchange ---
hyperliquid-python-sdk>=0.9,<1
eth-account>=0.13,<1

# --- Observability ---
structlog>=24.4,<25
python-json-logger>=2.0,<3

# --- WebSocket ---
websockets>=14.0,<15

# --- Sentiment & data ingestion ---
asyncpraw>=7.7,<8
vaderSentiment>=3.3,<4
pytrends>=4.9,<5
yfinance>=0.2,<1

# --- Regime classification ---
hmmlearn>=0.3,<1

# --- Dashboard / monitoring ---
fastapi>=0.115,<1
uvicorn[standard]>=0.34,<1
prometheus-client>=0.21,<1

# --- Utilities ---
orjson>=3.10,<4
python-dotenv>=1.0,<2
uvloop>=0.21,<1;platform_system!="Windows"

# --- Dev / test (install with: pip install -r requirements.txt -r requirements-dev.txt) ---
# pytest>=8.0
# pytest-asyncio>=0.24
# ruff>=0.8
